# Agent context

本仓库只负责 **120 张无大字新厦麻**。开始工作前依次阅读：

1. `README.md`
2. `HANDOFF.md`
3. `RULES_RESEARCH.md`
4. 涉及游金时再读 `docs/working-notes/double-triple-tour-flow.md`

## 不可破坏的系统边界

- 不增加 120/144 运行时开关，不把 144 张老厦麻代码、牌墙、底水或字牌跟打迁回来。
- 牌组固定为万筒条 108 张、白板 4 张、花牌 8 张；没有东南西北、中、发。
- 庄家起手 17 张，闲家 16 张；普通胡为五组牌加一对将。
- 真金是万能牌；白板不是第二张万能牌，只固定替代本局金牌的原牌面。
- 手里有真金不能胡他人弃牌；双金不能普通胡，必须进入游金；牌中三金不能按起手三金倒。
- 默认不支持七对。不要因为其他麻将常见就推断本玩法也支持。
- 房规差异必须显式标注和做成开关；证据不足时保持关闭。

## 实现纪律

- 规则判断以服务端 `xiamen_mahjong_120/` 为真相源；教学页面不能冒充后端实现。
- 游戏页和验牌页必须共用 `web_game_static/tile-ui.js`，避免同一张牌出现不同 UI。
- 新规则先补 `RULES_RESEARCH.md` 的证据与边界，再写失败测试，然后改引擎。
- 修改后至少运行 `python3 -m unittest discover -s tests -v`。
- 前端变化应启动 `python3 scripts/serve_web_game.py` 并实测相关页面。
- 不隐瞒未实现项：演示、部分实现、真实规则判断必须在 UI 和文档中清晰区分。

## 智能体与训练上下文

- `HeuristicTeacherAgent` 是当前可运行基线，使用牌形质量和听牌数选择动作。
- 用户另有 Ubuntu 26 训练主机，但本地尚无连接信息、权重或训练协议。
- 未来接入模型时，应保留启发式智能体作为可复现基线，并通过清晰接口替换决策器，
  不能把训练侧逻辑塞进规则状态机。
